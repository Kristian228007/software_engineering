services:
    poco_server:
        build: .
        ports:
            - "8080:8080"
        environment:
            PORT: 8080
            LOG_LEVEL: information
            JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
